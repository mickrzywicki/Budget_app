.card
  .card-header
    %h2
      = I18n.t('devise.sessions.name_on_button_submit')
      \&gt;
  .card-body
    = form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
      .field.form-group
        - f.label :email
        %br/
        = f.email_field :email, class: "form-control", placeholder: I18n.t('devise.sessions.name_on_placeholder_email'), autofocus: true, autocomplete: "email"
      .field.form-group
        - f.label :password
        %br/
        = f.password_field :password, class: "form-control", placeholder: I18n.t('devise.sessions.name_on_placeholder_password'), autocomplete: "current-password"
      - if devise_mapping.rememberable?
        .field.form-check
          = f.check_box :remember_me
          = f.label :remember_me
      .actions
        = f.submit I18n.t('devise.sessions.name_on_button_submit'), class: "btn btn-secondary"
    = render "devise/shared/links"
